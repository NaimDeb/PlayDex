/**
 * Application-wide configuration constants
 * @module constants/app
 */

/** Maximum game ID for random game selection */
export const MAX_GAME_ID = 120000;

/** Default number of items per page for pagination */
export const DEFAULT_PAGE_SIZE = 20;

/** Maximum number of visible page buttons in pagination */
export const PAGINATION_MAX_VISIBLE = 7;

/** Debounce delay for search input in milliseconds */
export const SEARCH_DEBOUNCE_DELAY = 400;

/** Debounce delay for general input fields in milliseconds */
export const INPUT_DEBOUNCE_DELAY = 400;

/** API request timeout in milliseconds */
export const API_TIMEOUT_MS = 30000;

/** Default locale for the application */
export const DEFAULT_LOCALE = 'fr-FR';

/** Date formatting locale */
export const DATE_LOCALE = 'fr-FR';
