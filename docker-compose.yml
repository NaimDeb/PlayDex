services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    # on injecte les secrets au runtime
    env_file:
      - ./backend/.env
    # clés JWT via un volume
    volumes:
      - ./backend/config/jwt:/app/config/jwt:ro
    environment:
      # Surcharge le DATABASE_URL du .env pour viser le conteneur "db" (et non 127.0.0.1).
      DATABASE_URL: "mysql://root:root@db:3306/playdex?serverVersion=8.4&charset=utf8mb4"

  frontend:
    build:
      context: ./frontend
      args:
        # Passé au BUILD (les variables NEXT_PUBLIC_* sont figées à ce moment-là).
        # Le navigateur appelle localhost:8000 = le port exposé par le backend.
        NEXT_PUBLIC_API_URL: "http://localhost:8000/api"
    ports:
      - "3000:3000"

  db:
    image: mysql:8.4
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: playdex
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-proot"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  db_data:
